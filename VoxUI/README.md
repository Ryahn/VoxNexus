# VOX UI (relocated)

The product SPA shell previously prototyped here now lives in **`apps/web`**.

Do not continue development in this folder. Extend `apps/web/src` (and extract
shared primitives into `packages/ui` over time) instead.

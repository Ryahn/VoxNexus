# Prefer the Compose env file next to the stack definition:
#   copy deploy/docker/.env.example deploy/docker/.env
#
# Or from the repo root:
#   docker compose -f deploy/docker/docker-compose.yml --env-file deploy/docker/.env.example up -d --build
